(()=>{'use strict';if((location.pathname.split('/').pop()||'').toLowerCase()!=='rep-sales-v2.html')return;const btn=document.getElementById('save');if(!btn||typeof btn.onclick!=='function')return;const original=btn.onclick;let saving=false;const units={bulk:['carton'],wholesale:['carton','dozen'],market:['carton','dozen','piece'],retail:['carton','dozen','piece']},labels={carton:'كارتون',dozen:'درزن',piece:'قطعة'};function allowed(){return units[tier()]||units.wholesale}function chosen(i){draft.units=draft.units||{};const u=String(draft.units[i.id]||i.salesUnit||allowed()[0]);return allowed().includes(u)?u:allowed()[0]}function syncItem(i){const u=chosen(i),raw=i._raw||rawById(i.id)||{},f=factorOf(raw,u);i.salesUnit=u;i.conversionFactor=f;i.price=priceOf(raw,u);i.priceMissing=!Number.isFinite(i.price);i.stock=Math.max(0,baseStock(raw)-pendingBase(i.id))/f}function unitSelect(i){const a=allowed(),u=chosen(i);return a.length>1?'<select data-unit="'+esc(i.id)+'" class="unit" style="width:100%;min-height:34px;border:1px solid #aebed0;border-radius:8px;margin-top:5px">'+a.map(x=>'<option value="'+x+'" '+(x===u?'selected':'')+'>'+labels[x]+'</option>').join('')+'</select>':'<div class="meta">الوحدة: '+labels[u]+'</div>'}function enhanced(i,picked=false){syncItem(i);const controls=picked?'<div class="picked-actions"><input class="qty" inputmode="numeric" type="number" min="0" step="1" value="'+(qty(i)||'')+'" data-id="'+esc(i.id)+'" '+(i.priceMissing?'disabled':'')+'><button class="remove" data-remove="'+esc(i.id)+'">حذف</button></div>':'<input class="qty" inputmode="numeric" type="number" min="0" step="1" value="'+(qty(i)||'')+'" placeholder="'+(i.priceMissing?'السعر غير محدد':'الكمية')+'" data-id="'+esc(i.id)+'" '+(i.priceMissing?'disabled':'')+'>';return '<div class="item"><div><div class="item-name">'+esc(i.name)+'</div><div class="meta">WH '+money(availableBaseId(i.id)/i.conversionFactor)+' &nbsp;&nbsp; P '+(i.priceMissing?'—':money(i.price))+'</div>'+unitSelect(i)+'</div>'+controls+'</div>'}window.row=enhanced;const oldBind=window.bind;window.bind=function(){oldBind();document.querySelectorAll('[data-unit]').forEach(s=>s.onchange=()=>{draft.units=draft.units||{};draft.units[s.dataset.unit]=s.value;save('albayanRepSalesDraftV2',draft);const i=items.find(x=>String(x.id)===String(s.dataset.unit));if(i)syncItem(i);renderAll();renderPicked()})};window.renderPicked=function(){const a=items.filter(i=>qty(i)>0);a.forEach(syncItem);document.getElementById('pickedCount').textContent=a.length;document.getElementById('pickedList').innerHTML=a.length?a.map(i=>{const pi=promoInfo(i),fq=freeQty2(i);return enhanced(i,true)+(fq?'<div class="item free"><div><div class="item-name">'+esc(pi.name)+' · FTF مجاني</div><div class="meta">WH '+money(availableBaseId(pi.fid)/pi.ff)+' &nbsp;&nbsp; P 0</div></div><b>'+fq+' '+label(pi.fu)+'</b></div>':'')}).join(''):'<div class="empty">بعدك ما اخترت مواد</div>';document.getElementById('total').textContent=money(a.reduce((s,i)=>s+qty(i)*(Number.isFinite(i.price)?i.price:0),0))+' د.ع';bind()};items.forEach(syncItem);renderAll();renderPicked();btn.onclick=function(e){if(saving)return;const selected=typeof items!=='undefined'?items.filter(i=>Number(qty(i))>0):[];selected.forEach(syncItem);for(const i of selected){const q=Number(qty(i));if(!Number.isSafeInteger(q)||q<=0){show('الكمية لازم تكون رقم صحيح للصنف: '+i.name,true);return}if(!allowed().includes(i.salesUnit)){show('وحدة البيع غير مسموحة لفئة الزبون: '+i.name,true);return}if(i.priceMissing){show('سعر '+labels[i.salesUnit]+' غير متوفر للصنف: '+i.name,true);return}}const live=load('albayanWorkspaceV1',{items:[]}),pending=load('albayanPendingInvoicesV1',[]);function rid(x,k){return String(x.id??x.code??k)}function liveBase(id){const r=(live.items||[]).find((x,k)=>rid(x,k)===String(id));return r?Number(r.quantity||0):0}function reservedBase(id){return (Array.isArray(pending)?pending:[]).filter(o=>{const st=String(o.status||'new').toLowerCase(),ss=String(o.syncStatus||'').toLowerCase();return st!=='cancelled'&&st!=='delivered'&&ss!=='cancelled'&&ss!=='error'&&ss!=='delivered'}).reduce((s,o)=>s+(o.invoice?.lines||[]).reduce((z,l)=>z+(String(l.itemId)===String(id)?Number(l.quantityBase)||Number(l.quantity||0)*Number(l.conversionFactor||1):0)+(String(l.freeItemId||l.itemId)===String(id)?Number(l.freeQuantityBase)||Number(l.freeQuantity||0)*Number(l.freeConversionFactor||l.conversionFactor||1):0),0),0)}for(const i of selected){const need=Number(qtyBase(i)||0),pi=promoInfo(i),fq=Number(freeQty2(i)||0),freeNeed=fq*Number(pi.ff||1),same=String(pi.fid)===String(i.id),available=Math.max(0,liveBase(i.id)-reservedBase(i.id));if(need+(same?freeNeed:0)>available+0.0001){show('المخزون تغير. الكمية غير كافية للصنف: '+i.name,true);return}if(fq>0&&!same){const freeAvailable=Math.max(0,liveBase(pi.fid)-reservedBase(pi.fid));if(freeNeed>freeAvailable+0.0001){show('المخزون تغير. الكمية المجانية غير كافية: '+pi.name,true);return}}}saving=true;btn.disabled=true;try{return original.call(this,e)}catch(err){console.error(err);saving=false;btn.disabled=false;show('تعذر حفظ الطلب بأمان. حاول مرة ثانية.',true)}};window.addEventListener('pageshow',()=>{if(!hasDraft()){saving=false;btn.disabled=false}})})();