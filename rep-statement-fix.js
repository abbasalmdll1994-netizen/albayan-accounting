(()=>{'use strict';if((location.pathname.split('/').pop()||'').toLowerCase()!=='representative.html')return;const money=n=>new Intl.NumberFormat('ar-IQ',{maximumFractionDigits:2}).format(Number(n)||0),RECEIPTS='albayanPaymentReceiptsV2';const load=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||'null')??f}catch{return f}},esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));function invoiceTotal(v){const lines=(v.lines||[]).reduce((s,l)=>s+Number(l.quantity||0)*Number(l.price??(l.unit==='carton'?l.cartonPrice:l.unit==='dozen'?l.dozenPrice:l.piecePrice)??0),Number(v.loading||0));return Number(v.total??v.grandTotal??lines??0)}window.invoiceTotal=invoiceTotal;function openReceipt(number){const r=load(RECEIPTS,[]).find(x=>String(x.number||x.id)===String(number));if(!r)return alert('تفاصيل سند القبض غير موجودة على هذا الجهاز');const old=document.getElementById('statementReceiptModal');if(old)old.remove();const m=document.createElement('div');m.id='statementReceiptModal';m.style.cssText='position:fixed;inset:0;background:#0b2138aa;z-index:99;display:flex;align-items:flex-end;justify-content:center;padding:12px';m.innerHTML=`<div style="background:#fff;width:min(520px,100%);border-radius:18px 18px 8px 8px;padding:18px"><div style="text-align:center"><h2 style="margin:0;color:#123b63">مكتب القرية</h2><h3>سند قبض</h3></div><p><b>رقم السند:</b> ${esc(r.number||r.id||'-')}</p><p><b>الزبون:</b> ${esc(r.customer||'')}</p><p><b>التاريخ:</b> ${esc(r.date||'')}</p><p><b>قيمة التسديد:</b> ${money(r.amount)} د.ع</p><p><b>الرصيد المتبقي:</b> ${money(r.after)} د.ع</p><div style="display:grid;grid-template-columns:1fr 1fr;gap:8px"><button class="btn primary" onclick="window.print()">🖨️ طباعة</button><button class="btn" id="statementReceiptClose">إغلاق</button></div></div>`;document.body.appendChild(m);m.querySelector('#statementReceiptClose').onclick=()=>m.remove()}window.openStatementReceipt=openReceipt;function render(){const name=(document.getElementById('statementName')?.textContent||document.getElementById('visitName')?.textContent||'').trim();if(!name||typeof window.workspace!=='function')return;const w=window.workspace(),accounts=(w.accounts||[]).filter(a=>a.customer===name),invoices=(w.invoices||[]).filter(v=>!v.cancelled&&v.customer===name),mov=[],postedOrders=new Set(accounts.filter(a=>String(a.sourceOrderId||'')).map(a=>String(a.sourceOrderId)));for(const v of invoices){const total=invoiceTotal(v);if(!total)continue;const sourceOrder=String(v.sourceOrderId||'');if(sourceOrder&&postedOrders.has(sourceOrder))continue;mov.push({date:v.date||'',createdAt:v.createdAt||'',type:'فاتورة',number:v.number||v.id||'-',amount:total,delta:total,rank:10})}for(const a of accounts){const by=Number(a.owedBy||0),to=Number(a.owedTo||0),src=String(a.source||''),meta=(String(a.type||'')+' '+String(a.notes||'')+' '+String(a.note||'')).toLowerCase();if(!by&&!to)continue;if(src==='payment-cash'||src==='payment-cash-reversal')continue;if(meta.includes('balance-correction')||meta.includes('تصحيح')&&meta.includes('رصيد')){mov.push({date:a.date||'',createdAt:a.createdAt||'',type:'تصحيح رصيد',number:'-',amount:Math.abs(by-to),delta:by-to,rank:15});continue}if(src==='representative-delivery'||src==='sale'||src==='invoice'||(by>0&&src!=='payment-reversal')){mov.push({date:a.date||'',createdAt:a.createdAt||'',type:'فاتورة',number:a.invoiceId||a.invoiceNo||a.documentNo||a.number||a.id||'-',amount:by,delta:by,rank:10});continue}if(src==='payment'){mov.push({date:a.date||'',createdAt:a.createdAt||'',type:'تسديد',number:a.receiptId||a.id||'-',amount:to,delta:-to,rank:20,receipt:true});continue}if(src==='payment-reversal'){mov.push({date:a.date||'',createdAt:a.createdAt||'',type:'إلغاء تسديد',number:a.receiptId||a.id||'-',amount:by,delta:by,rank:30});continue}if(to>0)mov.push({date:a.date||'',createdAt:a.createdAt||'',type:'تسديد',number:a.receiptId||a.id||'-',amount:to,delta:-to,rank:20})}const receiptNos=new Set(mov.filter(x=>x.type==='تسديد').map(x=>String(x.number)));for(const r of load(RECEIPTS,[])){if(r.customer!==name||r.status==='cancelled'||receiptNos.has(String(r.number||'')))continue;const amount=Number(r.amount||0);if(amount>0)mov.push({date:r.date||'',createdAt:r.createdAt||'',type:'تسديد',number:r.number||r.id||'-',amount,delta:-amount,rank:20,receipt:true})}mov.sort((a,b)=>String(a.date).localeCompare(String(b.date))||String(a.createdAt).localeCompare(String(b.createdAt))||a.rank-b.rank);let running=0;for(const x of mov){running+=x.delta;x.running=running}const rows=mov.map(x=>`<tr><td>${esc(x.date)}</td><td>${esc(x.type)}</td><td>${money(x.amount)}</td><td class="statement-running">${x.running<0?'له '+money(-x.running):money(x.running)}</td></tr>`).join('');const box=document.getElementById('statementRows');if(!box)return;box.innerHTML=`<div style="background:#f7fafc;border:1px solid #d9e3ed;border-radius:12px;padding:12px;margin-bottom:10px;display:flex;justify-content:space-between;align-items:center"><span>الرصيد الحالي</span><b style="font-size:20px">${running<0?'له '+money(-running):money(running)} د.ع</b></div><div style="overflow:auto"><table class="statement-table"><thead><tr><th>التاريخ</th><th>الحركة</th><th>المبلغ</th><th>الرصيد</th></tr></thead><tbody>${rows||'<tr><td colspan="4">لا توجد حركات</td></tr>'}</tbody></table></div><button class="btn statement-print" onclick="window.print()">🖨️ طباعة</button>`}function boot(){const b=document.getElementById('visitStatement');if(!b)return setTimeout(boot,150);b.addEventListener('click',()=>setTimeout(render,30));}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot):boot()})();