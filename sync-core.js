(()=>{const META='albayanSyncMetaV1',PROFILE='albayanRepProfileV1';function load(k,f){try{return JSON.parse(localStorage.getItem(k)||'null')??f}catch{return f}}function save(k,v){localStorage.setItem(k,JSON.stringify(v))}function id(prefix){return prefix+'-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,9)}function now(){return new Date().toISOString()}function profile(){const p=load(PROFILE,{id:'rep-1',name:'عباس'});return{id:String(p.id||'rep-1'),name:String(p.name||'عباس')}}function ensureRecord(r,prefix){if(!r||typeof r!=='object')return r;let changed=false;if(!r.id){r.id=id(prefix);changed=true}if(!r.createdAt){r.createdAt=now();changed=true}if(!r.updatedAt){r.updatedAt=r.createdAt||now();changed=true}if(!r.syncState){r.syncState='local';changed=true}return{record:r,changed}}function prepareArray(key,prefix){const a=load(key,[]);if(!Array.isArray(a))return{count:0,changed:0};let ch=0;for(let i=0;i<a.length;i++){const x=ensureRecord(a[i],prefix);if(x&&x.changed){a[i]=x.record;ch++}}if(ch)save(key,a);return{count:a.length,changed:ch}}function prepareWorkspace(){const key='albayanWorkspaceV1',w=load(key,{accounts:[],items:[],invoices:[]});let ch=0;for(const [name,prefix] of [['accounts','acc'],['items','item'],['invoices','inv']]){if(!Array.isArray(w[name]))w[name]=[];for(let i=0;i<w[name].length;i++){const x=ensureRecord(w[name][i],prefix);if(x&&x.changed){w[name][i]=x.record;ch++}}}if(ch)save(key,w);return{accounts:w.accounts.length,items:w.items.length,invoices:w.invoices.length,changed:ch}}function prepare(){const rp=profile(),ws=prepareWorkspace(),pending=prepareArray('albayanPendingInvoicesV1','pending'),handover=prepareArray('albayanCashHandoversV1','handover'),visits=prepareArray('albayanRepVisitsV2','visit'),meta=load(META,{}),stamp=now();meta.lastPreparedAt=stamp;meta.status='waiting-backend';meta.mode='local-ready';meta.repId=rp.id;meta.repName=rp.name;meta.schemaVersion=1;meta.counts={accounts:ws.accounts,items:ws.items,invoices:ws.invoices,pendingInvoices:pending.count,handovers:handover.count,visits:visits.count};meta.localChangesPrepared=ws.changed+pending.changed+handover.changed+visits.changed;save(META,meta);return meta}window.AlbayanSync={prepare,status:()=>load(META,{status:'not-prepared'}),markDirty:(record)=>{if(record&&typeof record==='object'){record.updatedAt=now();record.syncState='local';if(!record.id)record.id=id('rec');if(!record.createdAt)record.createdAt=record.updatedAt}return record}}})();