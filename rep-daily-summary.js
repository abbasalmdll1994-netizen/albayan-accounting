(()=>{'use strict';if((location.pathname.split('/').pop()||'').toLowerCase()!=='representative.html')return;const P='albayanPendingInvoicesV1',R='albayanPaymentReceiptsV2',E='albayanRepEndDayV1';const load=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||'null')??f}catch{return f}},save=(k,v)=>localStorage.setItem(k,JSON.stringify(v)),money=n=>new Intl.NumberFormat('ar-IQ',{maximumFractionDigits:0}).format(Number(n)||0)+' د.ع',today=()=>new Date().toISOString().slice(0,10);function boot(){const daily=document.getElementById('dailyContent');if(!daily)return setTimeout(boot,150);if(document.getElementById('repDaySummaryV2'))return;const box=document.createElement('section');box.id='repDaySummaryV2';box.className='panel';box.innerHTML='<h3 style="margin-top:0">ملخص الجولة اليوم</h3><div class="cards"><div class="card"><span class="muted">الطلبات</span><strong id="sumOrders">0</strong></div><div class="card"><span class="muted">قيمة البيع</span><strong id="sumSales">0 د.ع</strong></div><div class="card"><span class="muted">التحصيل</span><strong id="sumPay">0 د.ع</strong></div><div class="card"><span class="muted">بانتظار المزامنة</span><strong id="sumPending">0</strong></div></div><button id="finishRepDay" class="btn success wide">إنهاء الجولة</button><p id="finishRepMsg" class="muted" style="text-align:center;margin-bottom:0"></p>';daily.appendChild(box);document.getElementById('finishRepDay').onclick=finish;render();window.addEventListener('storage',render)}function orderTotal(p){return(p.invoice?.lines||[]).reduce((s,l)=>s+Number(l.quantity||0)*Number(l.price||0),0)}function render(){const d=today(),orders=load(P,[]).filter(x=>String(x.createdAt||'').slice(0,10)===d&&String(x.source||'').startsWith('representative')),receipts=load(R,[]).filter(x=>String(x.createdAt||x.date||'').slice(0,10)===d&&x.status!=='cancelled');document.getElementById('sumOrders').textContent=new Set(orders.map(x=>x.parentOrderId||x.id)).size;document.getElementById('sumSales').textContent=money(orders.reduce((s,x)=>s+orderTotal(x),0));document.getElementById('sumPay').textContent=money(receipts.reduce((s,x)=>s+Number(x.amount||0),0));document.getElementById('sumPending').textContent=orders.filter(x=>x.syncStatus==='pending').length+receipts.filter(x=>x.syncStatus==='pending').length;const ended=load(E,{})[d];if(ended){document.getElementById('finishRepDay').textContent='تم إنهاء الجولة';document.getElementById('finishRepDay').disabled=true;document.getElementById('finishRepMsg').textContent='محفوظة على الجهاز'+(Number(ended.pending||0)?' · توجد '+ended.pending+' حركة بانتظار المزامنة':'')}}function finish(){const d=today(),orders=load(P,[]).filter(x=>String(x.createdAt||'').slice(0,10)===d&&String(x.source||'').startsWith('representative')),receipts=load(R,[]).filter(x=>String(x.createdAt||x.date||'').slice(0,10)===d&&x.status!=='cancelled'),pending=orders.filter(x=>x.syncStatus==='pending').length+receipts.filter(x=>x.syncStatus==='pending').length,data=load(E,{});data[d]={endedAt:new Date().toISOString(),orders:new Set(orders.map(x=>x.parentOrderId||x.id)).size,sales:orders.reduce((s,x)=>s+orderTotal(x),0),collections:receipts.reduce((s,x)=>s+Number(x.amount||0),0),pending};save(E,data);render()}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot):boot()})();