(()=>{'use strict';function init(){const form=document.getElementById('saleForm');if(!form||form.dataset.priceFix)return;form.dataset.priceFix='2';form.addEventListener('submit',()=>{try{if(typeof state==='undefined'||typeof draftLines==='undefined')return;const tier=document.getElementById('saleTier')?.value||((typeof selectedTier!=='undefined')?selectedTier:'retail');let changed=false;for(const l of draftLines){if(!l||!l.itemId)continue;const item=state.items.find(i=>i.id===l.itemId),price=Number(l.cartonPrice);if(!item||!Number.isFinite(price)||price<=0)continue;if(!item.prices||typeof item.prices!=='object')item.prices={bulk:null,wholesale:null,market:null,retail:null};if(item.prices[tier]===null||item.prices[tier]===undefined||!Number.isFinite(Number(item.prices[tier]))||Number(item.prices[tier])<=0){item.prices[tier]=price;changed=true}l.cartonPrice=price;if(Number.isFinite(Number(l.packSize))&&Number(l.packSize)>0){l.piecePrice=Math.round((price/Number(l.packSize)+Number.EPSILON)*100)/100;l.dozenPrice=Math.round((price/Number(l.packSize)*12+Number.EPSILON)*100)/100}}if(changed){const raw=JSON.stringify(state);localStorage.setItem('albayanWorkspaceV1',raw);if(typeof storedSnapshot!=='undefined')storedSnapshot=raw}}catch(err){console.error('sales price fix',err)}},true)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init()})();