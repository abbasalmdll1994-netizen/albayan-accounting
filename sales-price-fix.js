(()=>{'use strict';function init(){const form=document.getElementById('saleForm');if(!form||form.dataset.priceFix)return;form.dataset.priceFix='1';form.addEventListener('submit',e=>{try{if(typeof state==='undefined'||typeof draftLines==='undefined'||typeof selectedTier==='undefined')return;let changed=false;for(const l of draftLines){if(!l?.itemId)continue;const item=state.items.find(i=>i.id===l.itemId);const price=Number(l.cartonPrice);if(item&&item.prices&&item.prices[selectedTier]===null&&Number.isFinite(price)&&price>0){item.prices[selectedTier]=price;changed=true}}if(changed){const raw=JSON.stringify(state);localStorage.setItem('albayanWorkspaceV1',raw);if(typeof storedSnapshot!=='undefined')storedSnapshot=raw}},true)}catch(err){console.error('sales price fix',err)}},true)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init()})();